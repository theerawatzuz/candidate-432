apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-thebrainsurf
  namespace: default
spec:
  secretName: wildcard-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.thebrainsurf.site"
    - "thebrainsurf.site"
