apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-nginx-config
data:
  nginx.conf: |
    events {}

    http {
      server {
        listen 80;
        
        location / {
          proxy_pass http://kpc-candidate-test.s3-website-ap-southeast-1.amazonaws.com;
        }
      }
    }
